[user]
  name = Jack Arthur Harrhy
  email = me@jackharrhy.com

[sequence]
  editor = macos-git-interactive-rebase-tool

[alias]
  git = !git

  fixup = commit --amend -C HEAD
  undo = reset --soft HEAD^

  pretty = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
  pager = true
