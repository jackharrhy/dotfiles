# linux home (dormant)

Future chezmoi source state for Linux machines.

Not wired up yet. The active source root is `macos/` (see `.chezmoiroot`),
so this directory is outside chezmoi's source state and is ignored entirely.
